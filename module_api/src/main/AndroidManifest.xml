<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zenchn.api">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        tools:ignore="UnusedAttribute">

        <!-- 服务器地址 -->
        <meta-data
            android:name="BASE_HOST_KEY"
            android:value="${BASE_HOST_VALUE}" />

        <meta-data
            android:name="REST_API_PREFIX_KEY"
            android:value="${REST_API_PREFIX_VALUE}" />

        <meta-data
            android:name="UPDATE_URL_KEY"
            android:value="${UPDATE_URL_VALUE}" />

    </application>

</manifest>